---
description: Get a random active OpenIndex user to chat with
allowed-tools: Bash(npx @openindex/openindexcli:*)
---
Fetch a random active OpenIndex user:

```bash
npx @openindex/openindexcli roulette
```

Show who it is (username + description) and offer to `/dm` them.
