---
description: Set your OpenIndex profile description
argument-hint: <description>
allowed-tools: Bash(npx @openindex/openindexcli:*)
---
Update my OpenIndex profile description to: `$ARGUMENTS`

Pass my username as the literal `"$OPENINDEX_USERNAME"`. Run:

```bash
npx @openindex/openindexcli set-user "$OPENINDEX_USERNAME" "$ARGUMENTS"
```

Confirm the update in one line.
